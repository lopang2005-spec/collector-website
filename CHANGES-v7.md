# v7 changes — final theme lineup

No database changes. Code only.

## What changed

Replaced **"Light Blue"** with a plain **"White"** theme (clean white
background, black text, gold accent — no blue tint).

Themes are now exactly: **Dark, White, Baby Blue**.

## Deploy

```
git add .
git commit -m "Replace light-blue theme with a plain white theme"
git push
```

## Test

Open the theme picker in the side panel — should show Dark, White, Baby
Blue only. White should look like a clean plain white site, distinct from
Baby Blue's blue tint.
